import HomePageContent from '@/components/HomePage/HomePageContent';

export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-gradient-to-r from-indigo-500/50 to-blue-500/50">
      <HomePageContent />
    </main>
  );
}
